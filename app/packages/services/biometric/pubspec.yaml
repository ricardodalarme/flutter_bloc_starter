name: biometric_service
version: 0.0.1

environment:
  sdk: ">=3.4.3 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  local_auth: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

