name: storybook
publish_to: 'none'

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  common_ui:
    path: ../packages/common_ui
  flutter:
    sdk: flutter
  widgetbook: ^3.4.1

dev_dependencies:
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
